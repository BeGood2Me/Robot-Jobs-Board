{
  "framework": "nextjs",
  "rootDirectory": "apps/web",
  "buildCommand": "pnpm --filter @robot-jobs-board/db generate && pnpm build",
  "installCommand": "pnpm install",
  "crons": [
    {
      "path": "/api/cron/ingest",
      "schedule": "0 9 * * *"
    }
  ]
}
